---
const {
  eyebrow,
  title,
  description,
} = Astro.props;
---

<section class="page-intro">
  <p class="eyebrow">{eyebrow}</p>
  <h1>{title}</h1>
  {description && <p class="intro-copy">{description}</p>}
</section>
